# Original reference files

원본 Flash 파일과 추출한 원본 자산은 이 폴더에서 로컬 전용으로 관리합니다.

예:

```text
original/
├── game.swf
└── extracted/
    ├── images/
    └── sounds/
```

`.gitignore`에 의해 원본 바이너리는 기본적으로 GitHub에 업로드되지 않습니다.
